name: edl-dashboard-infra
on: 
 workflow_dispatch:
env:
 def project_name = "edl-dashboard-infra"
 envNamesAndAccounts = []
 region = ""
 environments = [:]
 willDeployToLowerEnvironments = true || BRANCH_NAME == 'master'
 willDeployToProd
 snowChangeCreated = false
 buildFailed = false
 buildError
 approvalMap
